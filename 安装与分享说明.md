# 数字人视频技能（GUI 分享版）安装与分享说明

把飞影（HiFly）数字人视频生成做成了本地图形界面技能，双击即用，无需联网版扣子。

## 一、三步安装
1. **环境**：Windows 10+，已装 Node.js 18+（本机 WorkBuddy 自带 Node 22，一般无需另装；没有就去 nodejs.org 装 LTS）。
2. **解压**：把本文件夹（或解压 zip）放到 `C:\Users\<你的用户名>\.workbuddy\skills\feiying-digital-human\`。
3. **启动**：双击 `启动.bat` → 自动开浏览器到本地图形界面（默认 http://127.0.0.1:8787）。

## 二、填参数（界面右上角 ⚙ 设置）
- **飞影 API Token**：在飞影控制台「开放接口 / API 密钥」获取，填到设置面板保存（仅存本机技能根 `.env`，不随分享包发出）。
- **数字人 ID**（avatar）：形如 `av_xxxx`，飞影「我的数字人」页面复制。
- **声音 ID**（voice）：形如 `voice_xxxx`，可在设置面板「刷新声音列表」里选，或飞影控制台复制。
- **文案**（text）：要数字人播报的文字。
- 可选：字幕开关、去水印开关。

## 三、生成视频
1. 填好参数 → 点「生成」。
2. 界面每 8 秒自动轮询进度（对应原扣子工作流的循环查询）。
3. 状态变「完成」→ 结果视频卡出现预览 / 打开 / 复制链接 / 下载按钮；同时结果写入 `output/结果.md`。

## 四、与原扣子工作流的对应关系
- 开始节点 4 个参数 → 界面 4 个输入框（文案 / Token / 声音ID / 数字人ID）。
- 飞影 `create_lipsync_video2` 插件 → 本地 `POST /video/create_by_tts`。
- 循环查询状态节点 → 界面轮询 `GET /video/task`。
- 末尾 `create_document`（写飞书文档）节点 → 本地改为把结果追加写 `output/结果.md`（飞书需你方凭证，暂不自动写）。

## 五、安全提醒（分享前必看）
- 本分享包**不含任何真实 Token / Key**（技能根无 `.env`）。
- 你若自己填过 Token，分享给别人前请**删除你机器上的 `.env`**，或让对方自行在界面填。
- Token 只存在本机技能目录，不会上传任何服务器。

## 六、命令行调用（可选）
不打开界面也可：先 `config/example.env` 复制为 `.env` 填 `HIFLY_API_TOKEN`，再
`node gui/server.js`，按上面接口用 curl 调 `/api/generate`、`/api/task` 即可。
